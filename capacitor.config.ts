import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Votre Jardin Bien-être',
  webDir: 'www'
};

export default config;
